<Window x:Class="RevitAPI5.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPI5"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300"
        Title="Идентификатор элемента">
   
    <StackPanel>
        <Label Content="Выберите действие"
               Margin="5"/>
        <Button Content="Количество труб" Command="{Binding SelectCommand}"
                Margin="5"/>
        <!--<Button Content="Обхём стен" Command="{Binding WallCommand}"
                Margin="5"/>
        <Button Content="Количество дверей" Command="{Binding DoorCommand}"
                Margin="5"/>-->
    </StackPanel>
</Window>
